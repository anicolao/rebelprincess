rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // The game protocol has not been implemented yet. Keep production closed
    // until its document schema and rule tests are reviewed together.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
